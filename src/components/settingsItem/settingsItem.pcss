.item {
  width: 100%;
  display: flex;
  justify-content: space-between;
  padding: 10px 0;
  border-bottom: 1px solid var(--greyLight);
}

.item:last-child {
  border-bottom: none;
}

.item_value {
  color: var(--grey);
}
